# Usage: export $(cat .env.cuda)

# Environment variables for PyTorch CUDA development
# These must be set BEFORE importing PyTorch

# CUDA memory management
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:512

# CUDA optimizations
CUDA_LAUNCH_BLOCKING=0

# cuEquivariance optimizations (when kernels are enabled)
CUEQUI_FALLBACK_DEBUG=0
CUEQUI_PROFILE_LAUNCH=0
CUEQUI_MEMORY_POOL=1
# TODO - determine if necessary, or defining in pytorch sufficient
CUQUI_ENABLE_BF16=1

# General PyTorch optimizations
OMP_NUM_THREADS=8
TOKENIZERS_PARALLELISM=false

# Optional: Enable CUDA profiling (uncomment if needed)
# CUDA_PROFILE=1
# NVTX_PROFILE=1